{
  "name": "nodetest",
  "version": "1.0.0",
  "description": "Test package for Node",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "build:clean": "rm -rf ./dist && npm run build",
    "start": "ts-node src/index.ts",
    "test": "jest",
    "test:cov": "jest --coverage"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.2",
    "@types/node": "^20.3.1",
    "jest": "^29.5.0",
    "typescript": "^5.1.3"
  },
  "dependencies": {
    "ts-node": "^10.9.1"
  }
}
